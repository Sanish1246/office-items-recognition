## Getting Started

In order to install all dependencies, run the following command:

```
pip install -r ./requirements.txt
```

### Starting Web Server

In order to start the web server, run the following command:

```
fastapi dev server.py
```
